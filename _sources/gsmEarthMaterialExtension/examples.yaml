# Examples manifest for this building block.
# Each entry lists a title, descriptive content (Markdown),
# and one or more snippets pointing to example files.

examples:
  - title: examplegsmEarthMaterialExtensionMinimal
    content: "Minimal AlterationDescription instance — no required properties"
    snippets:
      - language: json
        ref: examplegsmEarthMaterialExtensionMinimal.json
