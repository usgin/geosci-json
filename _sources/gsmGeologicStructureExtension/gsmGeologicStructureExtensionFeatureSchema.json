{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.usgin.org/geosci-json/gsmGeologicStructureExtension/gsmGeologicStructureExtensionFeatureSchema.json",
  "description": "Single-Feature dispatcher for the gsmGeologicStructureExtension BB. Routes by the instance's `featureType` value to the matching $anchor in gsmGeologicStructureExtensionSchema.json. Recognised featureType values: DisplacementEvent, FoldSystem, Joint, Layering, Lineation, NonDirectionalStructure.",
  "allOf": [
    {
      "if": {
        "required": [
          "featureType"
        ],
        "properties": {
          "featureType": {
            "const": "DisplacementEvent"
          }
        }
      },
      "then": {
        "$ref": "gsmGeologicStructureExtensionSchema.json#DisplacementEvent"
      }
    },
    {
      "if": {
        "required": [
          "featureType"
        ],
        "properties": {
          "featureType": {
            "const": "FoldSystem"
          }
        }
      },
      "then": {
        "$ref": "gsmGeologicStructureExtensionSchema.json#FoldSystem"
      }
    },
    {
      "if": {
        "required": [
          "featureType"
        ],
        "properties": {
          "featureType": {
            "const": "Joint"
          }
        }
      },
      "then": {
        "$ref": "gsmGeologicStructureExtensionSchema.json#Joint"
      }
    },
    {
      "if": {
        "required": [
          "featureType"
        ],
        "properties": {
          "featureType": {
            "const": "Layering"
          }
        }
      },
      "then": {
        "$ref": "gsmGeologicStructureExtensionSchema.json#Layering"
      }
    },
    {
      "if": {
        "required": [
          "featureType"
        ],
        "properties": {
          "featureType": {
            "const": "Lineation"
          }
        }
      },
      "then": {
        "$ref": "gsmGeologicStructureExtensionSchema.json#Lineation"
      }
    },
    {
      "if": {
        "required": [
          "featureType"
        ],
        "properties": {
          "featureType": {
            "const": "NonDirectionalStructure"
          }
        }
      },
      "then": {
        "$ref": "gsmGeologicStructureExtensionSchema.json#NonDirectionalStructure"
      }
    },
    {
      "if": {
        "not": {
          "required": [
            "featureType"
          ],
          "properties": {
            "featureType": {
              "enum": [
                "DisplacementEvent",
                "FoldSystem",
                "Joint",
                "Layering",
                "Lineation",
                "NonDirectionalStructure"
              ]
            }
          }
        }
      },
      "then": false
    }
  ]
}
